# Agent

Agent表也不随着上线下线删除，也对应到不同的machineid

添加cascade限制

machine code可以在数据库里允许用户删除，agent的row要求对应的machine id row已经删除了

# suite run state

Stuie run state 不需要lost状态，都归类到Failed

suite_hook_execution改名exec/hook_task

run改名job

确认uuid生成器或者更改种子确保他们生成全局唯一

复用artifacts，不再使用单独的记录表；

给每个hook仍旧要分配一个uuid，只是它可以利用这个索引（以及可以通过这个来索引artifact来下载）

写一个小文档，说明现在的新设计，新交互

TODO：加一个clenaup，删除悬垂的artifacts（对应taskuuid不存在的）

先sync dev和main branch，新功能先合并到dev（允许force push）
